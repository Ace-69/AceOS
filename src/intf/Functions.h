#pragma once

/*uint8_t compare(char* ch1, char* ch2){
    uint8_t result = 1;
    uint8_t size = sizeof(ch1);
    if(size != sizeof(ch2)) {
        result = 0;
    } else {
        uint8_t i = 0;
        for(i; i <= size && result == 1; i++) {
            if(ch1[i] != ch2[i]) result = 0;
        }
    }
    return result;
}*/